#include <stdio.h>
#include <stdlib.h>
/* #include <../lib_ponto/lib_ponto.h> */

int main(){
    printf("Hello World\n");
    /* PONTO *p;
    PONTO *q;
    float *x1, *y1;
    float *x2, *y2;
    p = pto_cria(1,2);
    q = pto_cria(3.4,2.1);

    float d = pto_distancia(p,q);
    printf("O ponto 1 e: %.2f, %.2f\n", x1,y1);
    prtinf("O ponto 2 e: %.2f, %.2f\n", x2,y2);
    printf("A distancia entre os pontos e: %.2f\n", d);
    pto_libera(p);
    pto_libera(q);
    return 0; */
}